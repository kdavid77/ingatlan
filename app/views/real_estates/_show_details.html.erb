  <div class="span12">
    <h3>Részletek</h3>
    <% if @real_estate.detail.any? %>
      <table class="table table-hover table-condensed">
        <% @real_estate.detail.each do |det| %>
          <tr>
            <td><%= det.repar %></td>
            <td><%= det.reval %></td>
            <td><em><%= det.created_at %></em></td>
          </tr>
        <% end %>
      </table>
    <% end %>
    <div class="span12">
      <%= link_to "Szerkeszt", edit_detail_path, class: "btn btn-medium btn-primary" %>
    </div>
  </div>