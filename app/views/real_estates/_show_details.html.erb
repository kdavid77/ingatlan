<table class="table table-hover table-condensed">
  <tr><th>
    <% if cookies[:rs_show_details].nil? %>
      <% cookies[:rs_show_details] = 0 %>
      <%= link_to "+", real_estate_path(@real_estate), :onclick => 'document.cookie="rs_show_details=1" ' %>
    <% else %>
      <% if cookies[:rs_show_details]=="1" %>
        <%= link_to "-", real_estate_path(@real_estate), :onclick => 'document.cookie="rs_show_details=0" ' %>
      <% else %>
        <%= link_to "+", real_estate_path(@real_estate), :onclick => 'document.cookie="rs_show_details=1" ' %>
      <% end %>
    <% end %>
    Részletek
  </th></tr>

  <% if cookies[:rs_show_details]=="1" %>
    <% if @real_estate.detail.any? %>
      <% @real_estate.detail.each do |det| %>
        <tr>
          <td><%= det.repar %></td>
          <td><%= det.reval %></td>
          <td><em><%= det.created_at %></em></td>
        </tr>
      <% end %>
    <% end %>
  
    </table>

    <div class="span12">
      <%= link_to "Szerkeszt", edit_detail_path, class: "btn btn-small btn-primary" %>
    </div>
  <% else %>
    </table>
  <% end %>
