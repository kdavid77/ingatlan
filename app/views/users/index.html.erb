<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% users=User.all %>
Count: <%= users.count %> <br>
<% users.each do |user| %>
  <%= user.id %>
  --
  <%= user.name %>
  --
  <%= user.email %>
  --
  <%= user.password_digest %>
<% end %>
