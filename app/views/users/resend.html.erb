<%= provide(:title, "Resend confirmation") %> 

<%= form_for(:user, url: resent_path) do |f| %>
  <%= f.label :email %>
  <%= f.text_field :email %>
  <%= f.submit "Újraküldés", class:"bnt btn-primary btn-large" %>
<% end %>


